import React from "react";
import { Container } from "react-bootstrap";

const NotFoundPage = () => {
  return <Container style={{ height: "90vh", marginTop: "6rem" }}></Container>;
};

export default NotFoundPage;
